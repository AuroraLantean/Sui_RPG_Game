[package]
name = "RPG Game"
version = "0.0.1"
edition = "2024.beta"
# edition = "legacy" to use legacy (pre-2024) Move
# license = ""           # e.g., "MIT", "GPL", "Apache 2.0"
# authors = "John Snow (johnsnow@noemail.com)"]
#published-at = ""

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "testnet" }
# Revision can be a branch, a tag, and a commit hash; override = true

[addresses]
package_addr = "0x0"
#sui = "0x2"

[dev-addresses]

[dev-dependencies]
